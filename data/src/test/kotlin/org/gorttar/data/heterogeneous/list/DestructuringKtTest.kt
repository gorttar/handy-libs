/**
 * !!!This file is auto generated! All changes to it will be overwritten after running generator!!!
 */

package org.gorttar.data.heterogeneous.list

import assertk.assertAll
import assertk.assertThat
import assertk.assertions.isEqualTo
import org.junit.jupiter.api.Test

// @formatter:off

class DestructuringKtTest {
    @Test
    fun `xs1 destructuring`() = assertAll {
        val (a1) = xs1
        assertThat(a1).isEqualTo(a)
    }

    @Test
    fun `xs2 destructuring`() = assertAll {
        val (a1, b1) = xs2
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
    }

    @Test
    fun `xs3 destructuring`() = assertAll {
        val (a1, b1, c1) = xs3
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
    }

    @Test
    fun `xs4 destructuring`() = assertAll {
        val (a1, b1, c1, d1) = xs4
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
    }

    @Test
    fun `xs5 destructuring`() = assertAll {
        val (a1, b1, c1, d1, e1) = xs5
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
        assertThat(e1).isEqualTo(e)
    }

    @Test
    fun `xs6 destructuring`() = assertAll {
        val (a1, b1, c1, d1, e1, f1) = xs6
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
        assertThat(e1).isEqualTo(e)
        assertThat(f1).isEqualTo(f)
    }

    @Test
    fun `xs7 destructuring`() = assertAll {
        val (a1, b1, c1, d1, e1, f1, g1) = xs7
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
        assertThat(e1).isEqualTo(e)
        assertThat(f1).isEqualTo(f)
        assertThat(g1).isEqualTo(g)
    }

    @Test
    fun `xs8 destructuring`() = assertAll {
        val (a1, b1, c1, d1, e1, f1, g1, h1) = xs8
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
        assertThat(e1).isEqualTo(e)
        assertThat(f1).isEqualTo(f)
        assertThat(g1).isEqualTo(g)
        assertThat(h1).isEqualTo(h)
    }

    @Test
    fun `xs9 destructuring`() = assertAll {
        val (a1, b1, c1, d1, e1, f1, g1, h1, i1) = xs9
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
        assertThat(e1).isEqualTo(e)
        assertThat(f1).isEqualTo(f)
        assertThat(g1).isEqualTo(g)
        assertThat(h1).isEqualTo(h)
        assertThat(i1).isEqualTo(i)
    }

    @Test
    fun `xs10 destructuring`() = assertAll {
        val (a1, b1, c1, d1, e1, f1, g1, h1, i1, j1) = xs10
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
        assertThat(e1).isEqualTo(e)
        assertThat(f1).isEqualTo(f)
        assertThat(g1).isEqualTo(g)
        assertThat(h1).isEqualTo(h)
        assertThat(i1).isEqualTo(i)
        assertThat(j1).isEqualTo(j)
    }

    @Test
    fun `xs11 destructuring`() = assertAll {
        val (a1, b1, c1, d1, e1, f1, g1, h1, i1, j1, k1) = xs11
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
        assertThat(e1).isEqualTo(e)
        assertThat(f1).isEqualTo(f)
        assertThat(g1).isEqualTo(g)
        assertThat(h1).isEqualTo(h)
        assertThat(i1).isEqualTo(i)
        assertThat(j1).isEqualTo(j)
        assertThat(k1).isEqualTo(k)
    }

    @Test
    fun `xs12 destructuring`() = assertAll {
        val (a1, b1, c1, d1, e1, f1, g1, h1, i1, j1, k1, l1) = xs12
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
        assertThat(e1).isEqualTo(e)
        assertThat(f1).isEqualTo(f)
        assertThat(g1).isEqualTo(g)
        assertThat(h1).isEqualTo(h)
        assertThat(i1).isEqualTo(i)
        assertThat(j1).isEqualTo(j)
        assertThat(k1).isEqualTo(k)
        assertThat(l1).isEqualTo(l)
    }

    @Test
    fun `xs13 destructuring`() = assertAll {
        val (a1, b1, c1, d1, e1, f1, g1, h1, i1, j1, k1, l1, m1) = xs13
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
        assertThat(e1).isEqualTo(e)
        assertThat(f1).isEqualTo(f)
        assertThat(g1).isEqualTo(g)
        assertThat(h1).isEqualTo(h)
        assertThat(i1).isEqualTo(i)
        assertThat(j1).isEqualTo(j)
        assertThat(k1).isEqualTo(k)
        assertThat(l1).isEqualTo(l)
        assertThat(m1).isEqualTo(m)
    }

    @Test
    fun `xs14 destructuring`() = assertAll {
        val (a1, b1, c1, d1, e1, f1, g1, h1, i1, j1, k1, l1, m1, n1) = xs14
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
        assertThat(e1).isEqualTo(e)
        assertThat(f1).isEqualTo(f)
        assertThat(g1).isEqualTo(g)
        assertThat(h1).isEqualTo(h)
        assertThat(i1).isEqualTo(i)
        assertThat(j1).isEqualTo(j)
        assertThat(k1).isEqualTo(k)
        assertThat(l1).isEqualTo(l)
        assertThat(m1).isEqualTo(m)
        assertThat(n1).isEqualTo(n)
    }

    @Test
    fun `xs15 destructuring`() = assertAll {
        val (a1, b1, c1, d1, e1, f1, g1, h1, i1, j1, k1, l1, m1, n1, o1) = xs15
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
        assertThat(e1).isEqualTo(e)
        assertThat(f1).isEqualTo(f)
        assertThat(g1).isEqualTo(g)
        assertThat(h1).isEqualTo(h)
        assertThat(i1).isEqualTo(i)
        assertThat(j1).isEqualTo(j)
        assertThat(k1).isEqualTo(k)
        assertThat(l1).isEqualTo(l)
        assertThat(m1).isEqualTo(m)
        assertThat(n1).isEqualTo(n)
        assertThat(o1).isEqualTo(o)
    }

    @Test
    fun `xs16 destructuring`() = assertAll {
        val (a1, b1, c1, d1, e1, f1, g1, h1, i1, j1, k1, l1, m1, n1, o1, p1) = xs16
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
        assertThat(e1).isEqualTo(e)
        assertThat(f1).isEqualTo(f)
        assertThat(g1).isEqualTo(g)
        assertThat(h1).isEqualTo(h)
        assertThat(i1).isEqualTo(i)
        assertThat(j1).isEqualTo(j)
        assertThat(k1).isEqualTo(k)
        assertThat(l1).isEqualTo(l)
        assertThat(m1).isEqualTo(m)
        assertThat(n1).isEqualTo(n)
        assertThat(o1).isEqualTo(o)
        assertThat(p1).isEqualTo(p)
    }

    @Test
    fun `xs17 destructuring`() = assertAll {
        val (a1, b1, c1, d1, e1, f1, g1, h1, i1, j1, k1, l1, m1, n1, o1, p1, q1) = xs17
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
        assertThat(e1).isEqualTo(e)
        assertThat(f1).isEqualTo(f)
        assertThat(g1).isEqualTo(g)
        assertThat(h1).isEqualTo(h)
        assertThat(i1).isEqualTo(i)
        assertThat(j1).isEqualTo(j)
        assertThat(k1).isEqualTo(k)
        assertThat(l1).isEqualTo(l)
        assertThat(m1).isEqualTo(m)
        assertThat(n1).isEqualTo(n)
        assertThat(o1).isEqualTo(o)
        assertThat(p1).isEqualTo(p)
        assertThat(q1).isEqualTo(q)
    }

    @Test
    fun `xs18 destructuring`() = assertAll {
        val (a1, b1, c1, d1, e1, f1, g1, h1, i1, j1, k1, l1, m1, n1, o1, p1, q1, r1) = xs18
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
        assertThat(e1).isEqualTo(e)
        assertThat(f1).isEqualTo(f)
        assertThat(g1).isEqualTo(g)
        assertThat(h1).isEqualTo(h)
        assertThat(i1).isEqualTo(i)
        assertThat(j1).isEqualTo(j)
        assertThat(k1).isEqualTo(k)
        assertThat(l1).isEqualTo(l)
        assertThat(m1).isEqualTo(m)
        assertThat(n1).isEqualTo(n)
        assertThat(o1).isEqualTo(o)
        assertThat(p1).isEqualTo(p)
        assertThat(q1).isEqualTo(q)
        assertThat(r1).isEqualTo(r)
    }

    @Test
    fun `xs19 destructuring`() = assertAll {
        val (a1, b1, c1, d1, e1, f1, g1, h1, i1, j1, k1, l1, m1, n1, o1, p1, q1, r1, s1) = xs19
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
        assertThat(e1).isEqualTo(e)
        assertThat(f1).isEqualTo(f)
        assertThat(g1).isEqualTo(g)
        assertThat(h1).isEqualTo(h)
        assertThat(i1).isEqualTo(i)
        assertThat(j1).isEqualTo(j)
        assertThat(k1).isEqualTo(k)
        assertThat(l1).isEqualTo(l)
        assertThat(m1).isEqualTo(m)
        assertThat(n1).isEqualTo(n)
        assertThat(o1).isEqualTo(o)
        assertThat(p1).isEqualTo(p)
        assertThat(q1).isEqualTo(q)
        assertThat(r1).isEqualTo(r)
        assertThat(s1).isEqualTo(s)
    }

    @Test
    fun `xs20 destructuring`() = assertAll {
        val (a1, b1, c1, d1, e1, f1, g1, h1, i1, j1, k1, l1, m1, n1, o1, p1, q1, r1, s1, t1) = xs20
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
        assertThat(e1).isEqualTo(e)
        assertThat(f1).isEqualTo(f)
        assertThat(g1).isEqualTo(g)
        assertThat(h1).isEqualTo(h)
        assertThat(i1).isEqualTo(i)
        assertThat(j1).isEqualTo(j)
        assertThat(k1).isEqualTo(k)
        assertThat(l1).isEqualTo(l)
        assertThat(m1).isEqualTo(m)
        assertThat(n1).isEqualTo(n)
        assertThat(o1).isEqualTo(o)
        assertThat(p1).isEqualTo(p)
        assertThat(q1).isEqualTo(q)
        assertThat(r1).isEqualTo(r)
        assertThat(s1).isEqualTo(s)
        assertThat(t1).isEqualTo(t)
    }

    @Test
    fun `xs21 destructuring`() = assertAll {
        val (a1, b1, c1, d1, e1, f1, g1, h1, i1, j1, k1, l1, m1, n1, o1, p1, q1, r1, s1, t1, u1) = xs21
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
        assertThat(e1).isEqualTo(e)
        assertThat(f1).isEqualTo(f)
        assertThat(g1).isEqualTo(g)
        assertThat(h1).isEqualTo(h)
        assertThat(i1).isEqualTo(i)
        assertThat(j1).isEqualTo(j)
        assertThat(k1).isEqualTo(k)
        assertThat(l1).isEqualTo(l)
        assertThat(m1).isEqualTo(m)
        assertThat(n1).isEqualTo(n)
        assertThat(o1).isEqualTo(o)
        assertThat(p1).isEqualTo(p)
        assertThat(q1).isEqualTo(q)
        assertThat(r1).isEqualTo(r)
        assertThat(s1).isEqualTo(s)
        assertThat(t1).isEqualTo(t)
        assertThat(u1).isEqualTo(u)
    }

    @Test
    fun `xs22 destructuring`() = assertAll {
        val (a1, b1, c1, d1, e1, f1, g1, h1, i1, j1, k1, l1, m1, n1, o1, p1, q1, r1, s1, t1, u1, v1) = xs22
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
        assertThat(e1).isEqualTo(e)
        assertThat(f1).isEqualTo(f)
        assertThat(g1).isEqualTo(g)
        assertThat(h1).isEqualTo(h)
        assertThat(i1).isEqualTo(i)
        assertThat(j1).isEqualTo(j)
        assertThat(k1).isEqualTo(k)
        assertThat(l1).isEqualTo(l)
        assertThat(m1).isEqualTo(m)
        assertThat(n1).isEqualTo(n)
        assertThat(o1).isEqualTo(o)
        assertThat(p1).isEqualTo(p)
        assertThat(q1).isEqualTo(q)
        assertThat(r1).isEqualTo(r)
        assertThat(s1).isEqualTo(s)
        assertThat(t1).isEqualTo(t)
        assertThat(u1).isEqualTo(u)
        assertThat(v1).isEqualTo(v)
    }

    @Test
    fun `xs23 destructuring`() = assertAll {
        val (a1, b1, c1, d1, e1, f1, g1, h1, i1, j1, k1, l1, m1, n1, o1, p1, q1, r1, s1, t1, u1, v1, w1) = xs23
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
        assertThat(e1).isEqualTo(e)
        assertThat(f1).isEqualTo(f)
        assertThat(g1).isEqualTo(g)
        assertThat(h1).isEqualTo(h)
        assertThat(i1).isEqualTo(i)
        assertThat(j1).isEqualTo(j)
        assertThat(k1).isEqualTo(k)
        assertThat(l1).isEqualTo(l)
        assertThat(m1).isEqualTo(m)
        assertThat(n1).isEqualTo(n)
        assertThat(o1).isEqualTo(o)
        assertThat(p1).isEqualTo(p)
        assertThat(q1).isEqualTo(q)
        assertThat(r1).isEqualTo(r)
        assertThat(s1).isEqualTo(s)
        assertThat(t1).isEqualTo(t)
        assertThat(u1).isEqualTo(u)
        assertThat(v1).isEqualTo(v)
        assertThat(w1).isEqualTo(w)
    }

    @Test
    fun `xs24 destructuring`() = assertAll {
        val (a1, b1, c1, d1, e1, f1, g1, h1, i1, j1, k1, l1, m1, n1, o1, p1, q1, r1, s1, t1, u1, v1, w1, x1) = xs24
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
        assertThat(e1).isEqualTo(e)
        assertThat(f1).isEqualTo(f)
        assertThat(g1).isEqualTo(g)
        assertThat(h1).isEqualTo(h)
        assertThat(i1).isEqualTo(i)
        assertThat(j1).isEqualTo(j)
        assertThat(k1).isEqualTo(k)
        assertThat(l1).isEqualTo(l)
        assertThat(m1).isEqualTo(m)
        assertThat(n1).isEqualTo(n)
        assertThat(o1).isEqualTo(o)
        assertThat(p1).isEqualTo(p)
        assertThat(q1).isEqualTo(q)
        assertThat(r1).isEqualTo(r)
        assertThat(s1).isEqualTo(s)
        assertThat(t1).isEqualTo(t)
        assertThat(u1).isEqualTo(u)
        assertThat(v1).isEqualTo(v)
        assertThat(w1).isEqualTo(w)
        assertThat(x1).isEqualTo(x)
    }

    @Test
    fun `xs25 destructuring`() = assertAll {
        val (a1, b1, c1, d1, e1, f1, g1, h1, i1, j1, k1, l1, m1, n1, o1, p1, q1, r1, s1, t1, u1, v1, w1, x1, y1) = xs25
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
        assertThat(e1).isEqualTo(e)
        assertThat(f1).isEqualTo(f)
        assertThat(g1).isEqualTo(g)
        assertThat(h1).isEqualTo(h)
        assertThat(i1).isEqualTo(i)
        assertThat(j1).isEqualTo(j)
        assertThat(k1).isEqualTo(k)
        assertThat(l1).isEqualTo(l)
        assertThat(m1).isEqualTo(m)
        assertThat(n1).isEqualTo(n)
        assertThat(o1).isEqualTo(o)
        assertThat(p1).isEqualTo(p)
        assertThat(q1).isEqualTo(q)
        assertThat(r1).isEqualTo(r)
        assertThat(s1).isEqualTo(s)
        assertThat(t1).isEqualTo(t)
        assertThat(u1).isEqualTo(u)
        assertThat(v1).isEqualTo(v)
        assertThat(w1).isEqualTo(w)
        assertThat(x1).isEqualTo(x)
        assertThat(y1).isEqualTo(y)
    }

    @Test
    fun `xs26 destructuring`() = assertAll {
        val (a1, b1, c1, d1, e1, f1, g1, h1, i1, j1, k1, l1, m1, n1, o1, p1, q1, r1, s1, t1, u1, v1, w1, x1, y1, z1) = xs26
        assertThat(a1).isEqualTo(a)
        assertThat(b1).isEqualTo(b)
        assertThat(c1).isEqualTo(c)
        assertThat(d1).isEqualTo(d)
        assertThat(e1).isEqualTo(e)
        assertThat(f1).isEqualTo(f)
        assertThat(g1).isEqualTo(g)
        assertThat(h1).isEqualTo(h)
        assertThat(i1).isEqualTo(i)
        assertThat(j1).isEqualTo(j)
        assertThat(k1).isEqualTo(k)
        assertThat(l1).isEqualTo(l)
        assertThat(m1).isEqualTo(m)
        assertThat(n1).isEqualTo(n)
        assertThat(o1).isEqualTo(o)
        assertThat(p1).isEqualTo(p)
        assertThat(q1).isEqualTo(q)
        assertThat(r1).isEqualTo(r)
        assertThat(s1).isEqualTo(s)
        assertThat(t1).isEqualTo(t)
        assertThat(u1).isEqualTo(u)
        assertThat(v1).isEqualTo(v)
        assertThat(w1).isEqualTo(w)
        assertThat(x1).isEqualTo(x)
        assertThat(y1).isEqualTo(y)
        assertThat(z1).isEqualTo(z)
    }
}

// @formatter:on
